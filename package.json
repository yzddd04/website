{
  "name": "creator-monorepo",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "qrcode.react": "^4.2.0",
    "react-icons": "^5.5.0"
  }
}
